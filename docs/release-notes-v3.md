# v3 发布说明：待输出验证

## 状态与证据范围

本文不是已完成的 v1 → v2 → v3 效果评审。检查基线提交 `2d1b87d9539cd297c1ed1ce126081783d2bd0a13` 时，`tests/outputs/v1/`、`tests/outputs/v2/` 和 `tests/outputs/v3/` 均仅包含空的 `.gitkeep`，没有模型输出或运行清单。十份 `tests/personas/persona-*.json` 也为空文件。因此无法引用任何版本的实际建议，亦无法验证输入是否一致。

此前对话中的画像与规则设计不等于保存的执行结果。下文不将设计意图当作已实现效果，也不生成示意性的“前后原文”。

## 十份画像的逐版比较

每个单元格中的“缺失”均指 v1、v2、v3 三版都缺少对应画像的保存输出，而不是该版本没有推荐该内容。画像文件为空，故不从编号推断其背景。

| 画像文件 | 推荐行业 v1 → v2 → v3 | 岗位职能 v1 → v2 → v3 | 风险表述 v1 → v2 → v3 | 技能建议 v1 → v2 → v3 | 可引用的前后原文 |
| --- | --- | --- | --- | --- | --- |
| persona-01.json | 缺失 | 缺失 | 缺失 | 缺失 | 无 |
| persona-02.json | 缺失 | 缺失 | 缺失 | 缺失 | 无 |
| persona-03.json | 缺失 | 缺失 | 缺失 | 缺失 | 无 |
| persona-04.json | 缺失 | 缺失 | 缺失 | 缺失 | 无 |
| persona-05.json | 缺失 | 缺失 | 缺失 | 缺失 | 无 |
| persona-06.json | 缺失 | 缺失 | 缺失 | 缺失 | 无 |
| persona-07.json | 缺失 | 缺失 | 缺失 | 缺失 | 无 |
| persona-08.json | 缺失 | 缺失 | 缺失 | 缺失 | 无 |
| persona-09.json | 缺失 | 缺失 | 缺失 | 缺失 | 无 |
| persona-10.json | 缺失 | 缺失 | 缺失 | 缺失 | 无 |

## 汇总：规则意图与实际效果分开

### 调查驱动的变化

尚不能确认任何实际变化。此前提供的 AI 使用分布、技能信心、组织资源、职业不确定性和机会差异均为待填写占位符，不能作为调查证据。也没有 v2 输出可以核验。不得声称调查已使某类画像得到更好或不同的建议。

### 访谈驱动的变化

用户提供了五条定性访谈结论，以下仅是已经提出的设计映射，不代表代码或输出已实现：

| 访谈观察 | 拟议的 v3 变化 | 需要在实际输出中检查 |
| --- | --- | --- |
| AI 先改变任务，而非直接消除职业 | 按任务解释 AI 辅助方式和人类责任 | 是否避免从单项任务推断职业必然消失 |
| 效率提升可能伴随更高要求和工作量 | 增加工时、质量与产出预期的核实问题 | 是否停止承诺 AI 必然让工作更轻松 |
| 领域知识、验证与判断重要 | 建议可检查的领域成果、核验流程和决策说明 | 是否超越通用“学习提示词”建议 |
| 初级任务减少可能影响经验积累 | 提供独立尝试、AI 辅助、错误分析和反馈路径 | 是否针对经验不足者提供具体练习，且不冒充工作经历 |
| AI 收益受组织、语言、工具、培训和经验影响 | 标注资源条件并提供低资源替代路径 | 是否避免以学历、城市或公司类别猜测个人资源 |

这些访谈观察的日期、样本量、地区与抽样方法尚未提供，不能据此估计全国发生频率或推断个人结果。

### 哪些画像变化最多或最少

无法判断。缺少三版输出与运行配置，不能把没有证据解释为“没有变化”，也不能按画像背景猜测变化幅度。

### 回看 v1 是否过于简单或自信

无法对 v1 作具体判断：当前没有可引用的 v1 原文。后续应检查职业消失的确定性判断、效率等于减负、仅建议学习提示词、忽略入门练习机会以及默认所有用户有相同工具资源等问题；它们是检查项，不是已发现的问题。

## 完成评审所需证据

1. 十份非空、通过 schema 校验的固定合成画像。
2. 每份画像对应的 v1、v2、v3 真实输出，共三十份，失败运行须单独标记，不能用模拟输出替代。
3. 每版运行清单：源提交、画像哈希、提示词哈希、模型及参数、市场和研究快照、行动计划版本、输出规则版本。
4. 实际调查发现及其已知范围与限制。

资料齐备后，在上表中逐项列出行业和职能的增删、风险与技能建议的变化，附输出文件路径、字段路径和准确短引文。若技能建议由独立模块生成，须说明其版本是否同步变化。比较应区分规则变化、数据变化与模型随机性；不可仅凭先后顺序断言某项研究导致某条输出。
