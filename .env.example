# Planned server-only configuration. No runnable application exists yet.
# Copy to .env.local only after the application is implemented.
# Never prefix credentials with NEXT_PUBLIC_ or commit real values.
LLM_API_KEY=
# API root including the provider's version path, for example /v1.
LLM_BASE_URL=
LLM_MODEL=
